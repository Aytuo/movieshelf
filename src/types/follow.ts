/* ========================================================================== */
/*                                 FOLLOW                                     */
/* ========================================================================== */

export type FollowStats = {
  followerCount: number;
  followingCount: number;
  viewerIsFollowing: boolean;
};
